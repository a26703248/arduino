const int BUZEER_PIN = 8;
const int DELAY = 500;

void setup() {
  Serial.begin(9600);
  pinMode(BUZEER_PIN, OUTPUT);
  tone(BUZEER_PIN, 494);//Si
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 494);//Si
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 523);//Do
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 587);//Ro
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 587);//Ro
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 440);//La
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 494);//Si
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 440);//La
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 392);//So
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 392);//So
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 440);//La
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 494);//Si
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 440);//La
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 392);//So
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 392);//So
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 440);//La
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 440);//La
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 494);//Si
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 440);//La
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 440);//La
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 392);//So
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 440);//La
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 392);//So
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 440);//La
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 494);//Si
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 523);//Do
  delay(DELAY);
  noTone(BUZEER_PIN);
  tone(BUZEER_PIN, 587);//Ro
  delay(DELAY);
  noTone(BUZEER_PIN);
}

void loop() {
  
}
